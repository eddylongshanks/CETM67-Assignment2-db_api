version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/db_api/
file_exists_behavior: overwrite

hooks:
  AfterInstall:
   - location: script/setup
     timeout: 300
     runas: root
